diff --git a/MODULE.bazel b/MODULE.bazel
index b9241a0a40a3..556f62309321 100644
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -11,6 +11,13 @@ archive_override(
     url = "https://github.com/JetBrains/rules_kotlin/archive/30e89b8f9a246952e14eb6f56c2265a46bf4dc54.zip",
 )
 bazel_dep(name = "rules_java", version = "9.3.0")
+single_version_override(
+    module_name = "rules_java",
+    patch_strip = 1,
+    patches = [
+        "//build:rules_java_stub.patch",
+    ],
+)
 bazel_dep(name = "rules_jvm", version = "0.0.1")
 bazel_dep(name = "protobuf", version = "33.4")
 single_version_override(
@@ -64,7 +71,10 @@ bazel_dep(name = "toolchains_llvm", version = "1.6.0")
 single_version_override(
     module_name = "toolchains_llvm",
     patch_strip = 1,
-    patches = ["//build:toolchains_llvm-windows_support.patch"],  # Add Windows support, patch of https://github.com/bazel-contrib/toolchains_llvm/pull/642
+    patches = [
+        "//build:toolchains_llvm-windows_support.patch",
+        "//build:cc_wrapper.patch",
+    ],  # Add Windows support, patch of https://github.com/bazel-contrib/toolchains_llvm/pull/642
 )
 
 llvm = use_extension("@toolchains_llvm//toolchain/extensions:llvm.bzl", "llvm", dev_dependency = True)  # `dev_dependency = True` to avoid leaking the extension in modules using that module, see https://github.com/bazel-contrib/toolchains_llvm/issues/569
diff --git a/platform/build-scripts/bazel/MODULE.bazel b/platform/build-scripts/bazel/MODULE.bazel
index 662dfca6c526..af93186d7588 100644
--- a/platform/build-scripts/bazel/MODULE.bazel
+++ b/platform/build-scripts/bazel/MODULE.bazel
@@ -4,6 +4,13 @@ module(
 )
 
 bazel_dep(name = "rules_java", version = "9.3.0")
+single_version_override(
+    module_name = "rules_java",
+    patch_strip = 1,
+    patches = [
+        "//:build/rules_java_stub.patch",
+    ],
+)
 bazel_dep(name = "rules_kotlin", version = "2.2.2")
 archive_override(
     module_name = "rules_kotlin",
@@ -70,6 +77,30 @@ maven.install(
     ],
 )
 use_repo(maven, "j2b_maven")
+#bazel_dep(name = "toolchains_llvm", version = "1.6.0")
+#single_version_override(
+#    module_name = "toolchains_llvm",
+#    patch_strip = 1,
+#    patches = [
+#        "//:build/cc_wrapper.patch",
+#    ],
+#)
+
+#llvm = use_extension("@toolchains_llvm//toolchain/extensions:llvm.bzl", "llvm", dev_dependency = True)  # `dev_dependency = True` to avoid leaking the extension in modules using that module, see https://github.com/bazel-contrib/toolchains_llvm/issues/569
+#llvm.toolchain(
+#    name = "llvm_toolchain",
+#    llvm_version = "20.1.3",
+#    exec_os = "linux",
+#)
+
+# No sysroot is wired as this MODULE.bazel is public
+
+#use_repo(llvm, "llvm_toolchain", "llvm_toolchain_llvm")
+
+#register_toolchains(
+#    "@llvm_toolchain//:all",
+#    dev_dependency = True,
+#)  # `dev_dependency = True` to avoid registering toolchains in modules using that module, see https://github.com/bazel-contrib/toolchains_llvm/issues/569
 
 bazel_dep(name = "rules_python", version = "1.7.0")

